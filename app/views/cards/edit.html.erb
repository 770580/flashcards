<div class="container">
  <div class="jumbotron">
	<%= simple_form_for(@card, html: { class: '.form-inline' }) do |f| %>
	  <%= f.input :original_text, label: 'Слово' %>
	  <%= f.input :translated_text, label: 'Перевод' %>
	  <%= f.input :card_image, as: :file%>
	  <%= f.button :submit, "Редактировать" %>
	<% end %>
  </div>
</div>