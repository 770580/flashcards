<div class="container">
  <div class="jumbotron">
	<%= simple_form_for(@card, html: { class: '.form-inline' }) do |f| %>
	  <%= f.input :original_text, label: 'Your username please' %>
	  <%= f.input :translated_text %>
	  <%= f.button :submit, "Редактировать" %>
	<% end %>
  </div>
</div>